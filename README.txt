before running make sure you have
 angluar 6.x^ installed | to check type command in cmd ng -v
 node 8.x^		| to check type command in cmd node -v
 angular cli6.x^	| to check type command in cmd ng -v
 npm			| to check type command in cmd npm -v
***For Running it on your system****
Get it in your system.
get inside the project folder
open cmd
1.type the command npm install //it will install all dependencies and a node_module folder will be genrated
2.now type command ng serve
3.now check localhost:4200 in browser here it is hosted on your local machine.
